@* @page 
@model StudentAdvisor.Pages.Login.IndexModel
@{
    ViewData["Title"] = "Login";
    Layout = "_LoginLayout";
    

} *@

@* <h2>Login</h2>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <p class="text-danger">@Model.ErrorMessage</p>
}

<form method="post">
    <div>
        <label>Email:</label>
        <input asp-for="Logins.Email" class="form-control" />
        <span asp-validation-for="Logins.Email" class="text-danger"></span>
    </div>
    <div>
        <label>Password:</label>
        <input asp-for="Logins.Password" type="password" class="form-control" />
        <span asp-validation-for="Logins.Password" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Login</button>
</form>

 *@

@page 
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
